features = ["year",	"day" , "weekday", "hour", "season", "holiday", "workingday", "weather", "temp",
               	"humidity", "windspeed"]
target="count"
numerical_features = ['temp', 'humidity', 'windspeed', 'year', 'day', 'weekday', 'hour']
categorical_features = ['season', 'holiday', 'workingday', 'weather']
timestamp="datetime"
test_size=0.3
random_state=42

[random_forest]
n_estimators=200
max_depth=10
random_state=42
min_samples_split=10

[xgboost]
n_estimators=300
max_depth=5
eta=0.05
random_state=42

[ab_testing]
enabled = true
default_model = "random_forest"
log_dir = "logs/ab_testing"
metrics_dir = "evaluation/ab_testing"

[ab_testing.data_split]
train_ratio = 0.6
validation_ratio = 0.2
ab_test_ratio = 0.2
random_state = 42

[ab_testing.experiments.model_comparison_v1]
name = "Random Forest vs XGBoost Comparison"
status = "active"  # active, stopped, concluded
start_date = "2024-01-15"

[ab_testing.experiments.model_comparison_v1.variants.control]
model = "random_forest"
traffic_split = 0.5

[ab_testing.experiments.model_comparison_v1.variants.treatment]
model = "xgboost"
traffic_split = 0.5